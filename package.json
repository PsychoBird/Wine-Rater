{
  "scripts": {
    "start": "cd app; node server.js",
    "setup": "psql --username postgres -f scripts/setup.sql",
    "cleanup": "psql --username postgres -f scripts/cleanup.sql"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}